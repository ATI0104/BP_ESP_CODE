connection_string = "postgresql://postgres:postgres@192.168.2.4:5432/" # I know that the connection string can already contain the database name, but I want to keep it separate, because multiple databases are used and this saves on processing later
database_name = "pv_panels"
mqtt_broker_address = "192.168.2.4"
